spring:
   datasource:
      username: pet_stores
      password: Password1!
      url: jdbc:mysql://localhost:3306/pet_stores
   
   jpa:
      hibernate:
         ddl-auto: create
      show-sql: true
      defer-datasource-initialization: true
      
   
   sql:
      init:
         mode: always